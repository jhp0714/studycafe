services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: studycafe
      POSTGRES_USER: studycafe_user
      POSTGRES_PASSWORD: studycafe_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
